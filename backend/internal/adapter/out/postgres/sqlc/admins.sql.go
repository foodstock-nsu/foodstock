// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: admins.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createAdmin = `-- name: CreateAdmin :exec
INSERT INTO admins (
    id,
    login,
    password_hash,
    created_at
) VALUES (
    $1,
    $2,
    $3,
    $4
)
`

type CreateAdminParams struct {
	ID           pgtype.UUID
	Login        string
	PasswordHash string
	CreatedAt    pgtype.Timestamptz
}

func (q *Queries) CreateAdmin(ctx context.Context, db DBTX, arg CreateAdminParams) error {
	_, err := db.Exec(ctx, createAdmin,
		arg.ID,
		arg.Login,
		arg.PasswordHash,
		arg.CreatedAt,
	)
	return err
}

const getAdminByID = `-- name: GetAdminByID :one
SELECT
    id,
    login,
    password_hash,
    created_at
FROM admins
WHERE id = $1
`

func (q *Queries) GetAdminByID(ctx context.Context, db DBTX, id pgtype.UUID) (Admin, error) {
	row := db.QueryRow(ctx, getAdminByID, id)
	var i Admin
	err := row.Scan(
		&i.ID,
		&i.Login,
		&i.PasswordHash,
		&i.CreatedAt,
	)
	return i, err
}

const getAdminByLogin = `-- name: GetAdminByLogin :one
SELECT
    id,
    login,
    password_hash,
    created_at
FROM admins
WHERE login = $1
`

func (q *Queries) GetAdminByLogin(ctx context.Context, db DBTX, login string) (Admin, error) {
	row := db.QueryRow(ctx, getAdminByLogin, login)
	var i Admin
	err := row.Scan(
		&i.ID,
		&i.Login,
		&i.PasswordHash,
		&i.CreatedAt,
	)
	return i, err
}

const upsertAdmin = `-- name: UpsertAdmin :exec
INSERT INTO admins (
    id,
    login,
    password_hash,
    created_at
) VALUES (
    $1,
    $2,
    $3,
    $4
)
ON CONFLICT (login) DO UPDATE
SET password_hash = EXCLUDED.password_hash
`

type UpsertAdminParams struct {
	ID           pgtype.UUID
	Login        string
	PasswordHash string
	CreatedAt    pgtype.Timestamptz
}

func (q *Queries) UpsertAdmin(ctx context.Context, db DBTX, arg UpsertAdminParams) error {
	_, err := db.Exec(ctx, upsertAdmin,
		arg.ID,
		arg.Login,
		arg.PasswordHash,
		arg.CreatedAt,
	)
	return err
}
