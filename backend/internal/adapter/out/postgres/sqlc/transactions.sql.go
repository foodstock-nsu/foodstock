// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: transactions.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createTransaction = `-- name: CreateTransaction :exec
INSERT INTO transactions (
    id,
    order_id,
    sbp_transaction_id,
    amount,
    status,
    paid_at,
    created_at
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7
)
`

type CreateTransactionParams struct {
	ID               pgtype.UUID
	OrderID          pgtype.UUID
	SbpTransactionID string
	Amount           pgtype.Numeric
	Status           NullTransactionStatus
	PaidAt           pgtype.Timestamptz
	CreatedAt        pgtype.Timestamptz
}

func (q *Queries) CreateTransaction(ctx context.Context, db DBTX, arg CreateTransactionParams) error {
	_, err := db.Exec(ctx, createTransaction,
		arg.ID,
		arg.OrderID,
		arg.SbpTransactionID,
		arg.Amount,
		arg.Status,
		arg.PaidAt,
		arg.CreatedAt,
	)
	return err
}

const getTransactionByID = `-- name: GetTransactionByID :one
SELECT
    id,
    order_id,
    sbp_transaction_id,
    amount,
    status,
    paid_at,
    created_at
FROM transactions
WHERE id = $1
`

func (q *Queries) GetTransactionByID(ctx context.Context, db DBTX, id pgtype.UUID) (Transaction, error) {
	row := db.QueryRow(ctx, getTransactionByID, id)
	var i Transaction
	err := row.Scan(
		&i.ID,
		&i.OrderID,
		&i.SbpTransactionID,
		&i.Amount,
		&i.Status,
		&i.PaidAt,
		&i.CreatedAt,
	)
	return i, err
}

const getTransactionBySbpID = `-- name: GetTransactionBySbpID :one
SELECT
    id,
    order_id,
    sbp_transaction_id,
    amount,
    status,
    paid_at,
    created_at
FROM transactions
WHERE sbp_transaction_id = $1
`

func (q *Queries) GetTransactionBySbpID(ctx context.Context, db DBTX, sbpID string) (Transaction, error) {
	row := db.QueryRow(ctx, getTransactionBySbpID, sbpID)
	var i Transaction
	err := row.Scan(
		&i.ID,
		&i.OrderID,
		&i.SbpTransactionID,
		&i.Amount,
		&i.Status,
		&i.PaidAt,
		&i.CreatedAt,
	)
	return i, err
}

const listTransactions = `-- name: ListTransactions :many
SELECT
    id,
    order_id,
    sbp_transaction_id,
    amount,
    status,
    paid_at,
    created_at
FROM transactions
WHERE order_id = $1
`

func (q *Queries) ListTransactions(ctx context.Context, db DBTX, orderID pgtype.UUID) ([]Transaction, error) {
	rows, err := db.Query(ctx, listTransactions, orderID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Transaction
	for rows.Next() {
		var i Transaction
		if err := rows.Scan(
			&i.ID,
			&i.OrderID,
			&i.SbpTransactionID,
			&i.Amount,
			&i.Status,
			&i.PaidAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTransaction = `-- name: UpdateTransaction :exec
UPDATE transactions
SET
    status = $1,
    paid_at = $2
WHERE id = $3
`

type UpdateTransactionParams struct {
	Status NullTransactionStatus
	PaidAt pgtype.Timestamptz
	ID     pgtype.UUID
}

func (q *Queries) UpdateTransaction(ctx context.Context, db DBTX, arg UpdateTransactionParams) error {
	_, err := db.Exec(ctx, updateTransaction, arg.Status, arg.PaidAt, arg.ID)
	return err
}
