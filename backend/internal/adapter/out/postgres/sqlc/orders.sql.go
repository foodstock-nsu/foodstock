// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: orders.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createOrder = `-- name: CreateOrder :exec
INSERT INTO orders (
    id,
    location_id,
    status,
    total_price,
    created_at,
    paid_at
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6
)
`

type CreateOrderParams struct {
	ID         pgtype.UUID
	LocationID pgtype.UUID
	Status     OrderStatus
	TotalPrice pgtype.Numeric
	CreatedAt  pgtype.Timestamptz
	PaidAt     pgtype.Timestamptz
}

func (q *Queries) CreateOrder(ctx context.Context, db DBTX, arg CreateOrderParams) error {
	_, err := db.Exec(ctx, createOrder,
		arg.ID,
		arg.LocationID,
		arg.Status,
		arg.TotalPrice,
		arg.CreatedAt,
		arg.PaidAt,
	)
	return err
}

const getOrder = `-- name: GetOrder :one
SELECT
    id,
    location_id,
    status,
    total_price,
    created_at,
    paid_at
FROM orders
WHERE id = $1
`

func (q *Queries) GetOrder(ctx context.Context, db DBTX, id pgtype.UUID) (Order, error) {
	row := db.QueryRow(ctx, getOrder, id)
	var i Order
	err := row.Scan(
		&i.ID,
		&i.LocationID,
		&i.Status,
		&i.TotalPrice,
		&i.CreatedAt,
		&i.PaidAt,
	)
	return i, err
}

const listExpiredOrders = `-- name: ListExpiredOrders :many
SELECT
    id,
    location_id,
    status,
    total_price,
    created_at,
    paid_at
FROM orders
WHERE status = 'PENDING'
  AND created_at < (NOW() AT TIME ZONE 'utc') - INTERVAL '15 minutes'
    FOR UPDATE SKIP LOCKED
`

func (q *Queries) ListExpiredOrders(ctx context.Context, db DBTX) ([]Order, error) {
	rows, err := db.Query(ctx, listExpiredOrders)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Order
	for rows.Next() {
		var i Order
		if err := rows.Scan(
			&i.ID,
			&i.LocationID,
			&i.Status,
			&i.TotalPrice,
			&i.CreatedAt,
			&i.PaidAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listOrdersByLocationID = `-- name: ListOrdersByLocationID :many
SELECT
    id,
    location_id,
    status,
    total_price,
    created_at,
    paid_at
FROM orders
WHERE location_id = $1
LIMIT $2 OFFSET $3
`

type ListOrdersByLocationIDParams struct {
	LocationID pgtype.UUID
	Limit      int32
	Offset     int32
}

func (q *Queries) ListOrdersByLocationID(ctx context.Context, db DBTX, arg ListOrdersByLocationIDParams) ([]Order, error) {
	rows, err := db.Query(ctx, listOrdersByLocationID, arg.LocationID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Order
	for rows.Next() {
		var i Order
		if err := rows.Scan(
			&i.ID,
			&i.LocationID,
			&i.Status,
			&i.TotalPrice,
			&i.CreatedAt,
			&i.PaidAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listOrdersByStatus = `-- name: ListOrdersByStatus :many
SELECT
    id,
    location_id,
    status,
    total_price,
    created_at,
    paid_at
FROM orders
WHERE status = $1
LIMIT $2 OFFSET $3
`

type ListOrdersByStatusParams struct {
	Status OrderStatus
	Limit  int32
	Offset int32
}

func (q *Queries) ListOrdersByStatus(ctx context.Context, db DBTX, arg ListOrdersByStatusParams) ([]Order, error) {
	rows, err := db.Query(ctx, listOrdersByStatus, arg.Status, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Order
	for rows.Next() {
		var i Order
		if err := rows.Scan(
			&i.ID,
			&i.LocationID,
			&i.Status,
			&i.TotalPrice,
			&i.CreatedAt,
			&i.PaidAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateOrder = `-- name: UpdateOrder :exec
UPDATE orders
SET
    status = $1,
    total_price = $2,
    paid_at = $3
WHERE id = $4
`

type UpdateOrderParams struct {
	Status     OrderStatus
	TotalPrice pgtype.Numeric
	PaidAt     pgtype.Timestamptz
	ID         pgtype.UUID
}

func (q *Queries) UpdateOrder(ctx context.Context, db DBTX, arg UpdateOrderParams) error {
	_, err := db.Exec(ctx, updateOrder,
		arg.Status,
		arg.TotalPrice,
		arg.PaidAt,
		arg.ID,
	)
	return err
}
